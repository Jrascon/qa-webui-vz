This directory contains the images and result .html pages generated by ResembleJS.

The base images used for comparison need to be stored in:

	./images/baseline_images/

The generated test and diff images will get saved to the following, respectively:

	./images/test_images/
	./images/diff_images/
	